           ANOTAÇÕES, TERMOS & STRINGS DAS AULAS DE IAC/ IDW & ALG&PROG:1                
                        
                            ==> HISTORIA DA COMPUTAÇÃO <==
HISTORIA Da COMPUTAÇÃO{
    PROTOCOLO => idioma único responsável pela comunicação de maquínas na rede;
    TCP => protocolo criado por:  ROBERT KAHN, com o objetivo de interligar diferentes computadores
        em uma mesma rede. Este protodolo, o TCP, tratava os dados de forma diferente pois quebrava-os em 
        pacotes, subistituindo assim em 1973 o NCP, da DARPANET ;
    FTP => protocolo respónsavel pela transmissão de arquivos na rede;
    DNS => protocolo responsável pela identificação de maquinas
        na rede;
    RAY TOMLINSON => modifiou alguns sitemas em 1972, criando o e-mail;
    ROBERT KAHN => criador do protocolo TCP e do termo INTERNETTING;
    VINTON SERF => criou o protocolo IP;
    TCP/IP => junção dos protocolos TCP & IP, permitindo entao muitos computadores em rede,
    TIM BERNERS LEE => criou o protocolo HTTP, respónsavel pela transferencia de hipertextos,
        Criou o HTML, que é a linguagem para a criação de conteudo dentro do hipertextos,
        Criou a World Wide Web, ou WWW, na qual engloba todos os conteudo multimidia dentro do protocolo HTTP;
    W3C => Wor Web Consortium, criada em 1995 após o lancamento do HTML:2, com o objetivo de padronizar as 
        tags usadas nos diferentes navegadores da epóca;
    MODEN => converte uma onda digital em  analogica, na saída do pc para rede, porcesso chamado de MODULAÇÃO, e quando esta
        onda analogica chega até o pc o mesmo MODEN faz o processo inveros ao primeiro convertendo a aonla que chega aálogica 
        em onda digítal, a este processo chamamos de DEMODULAÇÃO; 
    
}

                                ==> HTML <==
{

}
                                ==> CSS <==
{

}
                                 ==> JS <==
ESCREVER NO CONSOLE{
    console.log("") => VAI ESCREVER NO TERMINAL O Q TIVER DENTRO DO ("");
    console.log()   => VAI CHAMAR A VARIAVEL QUE ESTIVER DENTRO DO ();
} 
    
VARIAVEIS{
    const .....  => DETERMINA UMA VARIAVEL NUMERO;
    const ..... " " => DETERMINA UMA VARIAVEL DO TIPO NOME;
    let ...
    parseInt ..... // parseFloat.....=> Converte UMA VARIAVEL "NUMERO" 
        PARA UN TIPO INTEIRO OU REAL;
}